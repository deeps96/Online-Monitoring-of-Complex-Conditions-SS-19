FORALL pat_id.
(EXISTS d_id. (
   dental_on(d_id, pat_id)
  AND
   ONCE dental(d_id)
  )
)
IMPLIES (
 EXISTS p_id, a_id.
   (ONCE pump_on(p_id, pat_id))
  AND
   (ONCE action_by(a_id, p_id))
  AND
   (ONCE action_type(a_id, 'antibiotics'))
)
