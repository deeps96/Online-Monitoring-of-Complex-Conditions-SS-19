pred
  patient(pat_id),
  pump(p_id),
  action(a_id),
  dental(d_id),
  patient_id(pat_id, name),
  pump_on(p_id, pat_id),
  action_by(a_id, p_id),
  action_type(a_id, type),
  dental_on(d_id, pat_id)

prop before: Forall d_id . Forall pat_id . (dental(d_id) & dental_on(d_id, pat_id)) -> P Exists a_id . Exists p_id . (pump(p_id) & pump_on(p_id, pat_id) & action(a_id) & action_by(a_id, p_id) & action_type(a_id, "antibiotics"))
